<h2><%= @artist.name.titleize %></h2>

<h4>Song(s):</h4>
<% @artist.songs.each do |song| %>
<a href="/songs/<%= song.slug %>"><%= song.name %></a>
<% end %>

<h4>Genre(s):</h4>
<% @artist.genres.each do |genre| %>
<a href="/genres/<%= genre.slug %>"><%= genre.name.titleize %></a>
<% end %>